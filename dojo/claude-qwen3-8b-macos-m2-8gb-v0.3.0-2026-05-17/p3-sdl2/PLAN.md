## Files
- [x] main.c — SDL2 program to render a line on screen
- [x] Makefile — Build system with sdl2-config

## Test Command
make

## Dependencies
- clang (for C compilation)
- sdl2-config (for SDL2 setup)
- clang-tidy (for C linting)
