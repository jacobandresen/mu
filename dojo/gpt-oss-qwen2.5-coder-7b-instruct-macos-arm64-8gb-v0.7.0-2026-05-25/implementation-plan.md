# Implementation Plan – Dojo Improvements

## Action Items (one per iteration)

- [ ] Add rule to `skills/task-planner/SKILL.md` to automatically generate `requirements.txt` for Python goals and include a `Makefile` for any build/install mentions