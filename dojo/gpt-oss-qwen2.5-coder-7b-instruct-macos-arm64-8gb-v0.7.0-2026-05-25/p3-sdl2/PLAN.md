## Summary
Implement a simple SDL2 application that renders a line on the screen. Use `sdl2-config` to set up the necessary libraries and flags in the Makefile. Correctness will be verified by running the executable and checking if the line is rendered as expected.

## Files
- [x] main.c — C source code for the SDL2 application
- [x] Makefile — build rules using `sdl2-config`

## Test Command
make && ./main

## Dependencies
clang, make, sdl2-config