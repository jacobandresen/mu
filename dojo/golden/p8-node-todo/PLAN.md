## Summary
Implement a Node.js command-line tool to manage a todo list using a JSON file. The tool should support adding, listing, and deleting todos. Use Jest for testing and a Makefile to install dependencies and run tests.

## Files
- [ ] package.json — Node.js project metadata and dependencies
- [ ] index.js — Main entry point defining `TodoManager` class with `add`, `list`, and `delete` methods
- [ ] todo-test.js — Jest test file for the `TodoManager` class

## Test Command
npm test

## Dependencies
node>=14, npm>=7, jest>=26