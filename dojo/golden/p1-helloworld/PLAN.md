## Summary
Create a simple C program that prints "Hello, World!" using the `printf` function from the standard library. Compile the program using Clang and run it to verify correctness.

## Files
- [ ] main.c — C source file defining `main` that prints "Hello, World!"
- [ ] Makefile — build rules producing the `hello_world` binary

## Test Command
make && ./hello_world

## Dependencies
clang>=14, make>=4